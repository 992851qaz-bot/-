# 我的網站

一個現代化、響應式的中文靜態網站。

## 檔案結構

```
index.html   # 主頁面
styles.css   # 樣式表
script.js    # JavaScript（滾動動畫、計數器等）
```

## 功能

- 固定導覽列（毛玻璃效果）
- Hero 區塊（漸層標題、行動按鈕）
- 功能特色卡片（滾動顯現動畫）
- 數據統計（數字計數動畫）
- CTA 區塊
- 響應式佈局（支援手機）

## 開啟方式

直接在瀏覽器中開啟 `index.html` 即可，無需任何建置步驟。
